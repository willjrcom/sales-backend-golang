# Transmitenota API Configuration
# Copy these variables to your .env.local file

# Transmitenota API base URL
TRANSMITENOTA_API_URL=https://api.transmitenota.com.br

# Enable sandbox mode for testing (true/false)
TRANSMITENOTA_SANDBOX=true

# API request timeout
TRANSMITENOTA_TIMEOUT=30s

# REQUIRED: Transmitenota API credentials (centralized for all companies)
# These credentials are used for all NFC-e emissions
TRANSMITENOTA_USUARIO=your_api_username
TRANSMITENOTA_SENHA=your_api_password

# Note: Each company must still configure their own fiscal data via the UI:
# - inscricao_estadual (State Registration)
# - regime_tributario (1=Simples, 2=Simples Excesso, 3=Normal)
# - cnae (Economic Activity Code)  
# - crt (Tax Regime Code: 1, 2, or 3)
